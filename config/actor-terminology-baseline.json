{
  "$comment": "Narrow exceptions to the Companion/Partner/Operator/Participant/primary vocabulary gate. Every entry must identify an actual technical or persisted-contract use and carry a review note; stale entries fail the gate.",
  "entries": [
    {
      "path": "docs/process/maintenance-scripts.md",
      "pattern": "generic-user",
      "contains": "`{{user}}`, `the user`, `the companion`",
      "note": "Documents the literal placeholder strings the participant-name repair query matches; it does not name the Partner role."
    },
    {
      "path": "docs/process/maintenance-scripts.md",
      "pattern": "generic-user",
      "contains": "the definite labels `the user`, `Partner`, `the companion`,",
      "note": "Documents the literal placeholder strings the participant-name repair query matches; it does not name the Partner role."
    },
    {
      "path": "src/core/cogsec/intake-firewall-notice-templates.ts",
      "pattern": "relational-human",
      "contains": "'being kept aside for your human to look over',",
      "note": "Persisted pre-vocabulary notices retain this detection signature so they remain excluded from emotion and memory candidacy."
    },
    {
      "path": "src/faculties/memory/extraction/naming.ts",
      "pattern": "primary-as-partner",
      "contains": "  'primary user',",
      "note": "Legacy extracted-memory placeholder accepted as input so it can be rewritten to a real participant name."
    },
    {
      "path": "src/faculties/memory/extraction/naming.ts",
      "pattern": "primary-as-partner",
      "contains": "  'the primary user',",
      "note": "Legacy extracted-memory placeholder accepted as input so it can be rewritten to a real participant name."
    },
    {
      "path": "src/faculties/memory/extraction/naming.ts",
      "pattern": "primary-as-partner",
      "contains": "{ pattern: /\\bthe primary user's\\b/gi",
      "note": "Compatibility normalizer recognizes possessive legacy memory text; it does not generate this wording."
    },
    {
      "path": "src/faculties/memory/extraction/naming.ts",
      "pattern": "primary-as-partner",
      "contains": "{ pattern: /\\bthe primary user\\b(?!-)/gi",
      "note": "Compatibility normalizer recognizes legacy memory text; it does not generate this wording."
    },
    {
      "path": "src/persistence/repair/memory-participant-name-repair.ts",
      "pattern": "primary-as-partner",
      "contains": "OR lower(text) LIKE '%primary user%'",
      "note": "Operator repair query locates persisted legacy placeholder text for content-preserving participant-name repair."
    }
  ]
}
