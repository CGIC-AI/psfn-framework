{
  "schemaVersion": 1,
  "layers": [
    {
      "identifier": "runtime.attention",
      "name": "Runtime Attention",
      "priority": 100,
      "required": false,
      "content": "<runtime_attention>\n<open_threads>\n{{runtime_concerns_top_lines}}\n</open_threads>\n\n<behavioral_notes>\n{{runtime_behavioral_notes_body}}\n</behavioral_notes>\n\n<skills_index>\n{{runtime_skills_index_body}}\n</skills_index>\n</runtime_attention>"
    },
    {
      "identifier": "runtime.response_style",
      "name": "Runtime Response Style",
      "priority": 105,
      "required": false,
      "content": "<response_style_guidance>\n<style>{{runtime_response_style}}</style>\n{{#if runtime_response_style_is_concise}}<delivery>Answer directly and keep wording tight.</delivery>\n<expansion>Expand only when the user asks for more detail.</expansion>{{/if}}{{#if runtime_response_style_is_expressive}}<delivery>Keep your voice warm and vivid.</delivery>\n<expansion>Add personality-rich detail when it helps clarity.</expansion>{{/if}}\n</response_style_guidance>"
    },
    {
      "identifier": "runtime.tooling",
      "name": "Runtime Tooling",
      "priority": 110,
      "required": false,
      "content": "<runtime_tooling>\n<self_image_tool_guidance>\n{{#if runtime_self_image_tool_active}}Use selfie_create for new selfies, portraits, or self-representation. Preserve stable appearance cues from the character appearance section unless the user explicitly asks to vary them. When a saved reference photo exists, selfie_create uses the default reference through image edit by default; set use_reference_image=false only when the user wants a free generation, or pick a saved variant with reference_image_id/reference_image_tags. Write the prompt as the complete shot, combining identity, pose, camera angle, expression, lighting, environment, mood, and style. Use media action=\"generate\" for non-self images, media action=\"edit\" for transforming an existing image, and media action=\"analyze\" only for explicit URLs or generated outputs. If the current user message already includes attached image bytes, inspect that attachment directly instead of passing a stale URL. Generated image tools already return a vision review; use it before asking the user whether the result looks right, unless you need their subjective preference.{{/if}}\n</self_image_tool_guidance>\n\n<extended_tools>\n{{#if runtime_extended_tools_total}}Never claim a tool executed, failed, or was denied unless this turn contains the actual tool call and tool result.\nIf a non-default tool is not already active, activate it before you describe its outcome.\nCore tools are already active through the structured tool registry and are not duplicated here.\n{{runtime_extended_tool_directory_lines}}{{/if}}\n</extended_tools>\n</runtime_tooling>"
    },
    {
      "identifier": "runtime.state",
      "name": "Runtime State",
      "priority": 120,
      "required": true,
      "content": "<runtime_state>\n<last_message_received>\n<weekday>{{runtime_last_message_received_weekday}}</weekday>\n<date>{{runtime_last_message_received_date_human}}</date>\n<time>{{runtime_last_message_received_time_human}}</time>\n<timezone>{{runtime_last_message_received_timezone}}</timezone>\n<elapsed_time_since_last>{{runtime_last_message_received_ago}}</elapsed_time_since_last>\n<status>{{runtime_last_message_received_missing_notice}}</status>\n</last_message_received>\n\n<internal_turn_context>\n<kind>{{runtime_internal_turn_kind}}</kind>\n</internal_turn_context>\n\n<conversation_state>\n<chat_type>{{runtime_chat_type}}</chat_type>\n<channel_id>{{runtime_room_id}}</channel_id>\n<channel_type>{{runtime_channel_type}}</channel_type>\n<channel_visibility>{{runtime_channel_visibility}}</channel_visibility>\n{{runtime_current_message_author_xml}}\n{{runtime_recent_active_participants_xml}}\n</conversation_state>\n</runtime_state>"
    }
  ]
}
