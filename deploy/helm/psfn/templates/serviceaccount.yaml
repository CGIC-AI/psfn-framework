{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "psfn.serviceAccountName" . }}
  labels:
    {{- include "psfn.labels" . | nindent 4 }}
{{- end }}
