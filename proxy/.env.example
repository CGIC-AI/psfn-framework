# Real API keys — never exposed to agent process
OPENROUTER_API_KEY=sk-or-v1-...

# LiteLLM master key — used to authenticate virtual keys
# Generate with: openssl rand -hex 32
LITELLM_MASTER_KEY=sk-litellm-master-...
