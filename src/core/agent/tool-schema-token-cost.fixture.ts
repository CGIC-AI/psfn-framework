/** Fixed descriptions serialized by agent.state.tools at origin/main 190768ee9b. */
export const PRE_DESCRIPTION_REWRITE_TOOL_DESCRIPTIONS: Readonly<Partial<Record<string, string>>> = {
  beads: 'Unified tracked-work surface for beads issue discovery and mutation. Use action=ready|show|create|update|close|sync. Use id as a plain string such as "PSFN-123" for action=show|update|close; do not pass the whole ready payload or issue object. If you need an id from action=ready, call ready first, read its tool result, then call show/update/close in a later assistant step. Legacy issue_* aliases remain accepted as action values only.',
  fs: 'Unified filesystem primitive for personal-file inspection and safe mutation. Use action=list|read|search|write|edit. Prefer list/search/read for common file inspection before analysis_workbench. For action=search, omit glob for the default working-folder search or provide a narrow folder/file glob; do not use bare **/*. Write/edit paths are relative to the configured personal files root, not DATA or runtime state. In gateway yolo mode, reads may expose broader codebase paths while writes stay personal-root-relative. Writes stay explicit, bounded, and fail closed on unsafe overwrite/edit requests.',
  repo: 'Unified repository primitive for git-backed inspection and mutation. Use action=inspect before mutating; target=status|diff|both controls inspection detail. action=patch requires file_path and full replacement content; action=branch requires name; action=commit requires message, and action=publish/open_pr requires title/body. Inspect is read-only; patch, branch, commit, and publish remain explicitly gated.',
  journal: 'Durable markdown journal for companion-authored notes, reflections, and longer-lived context that is not a typed memory or active open thread. Use action=list|read|write|append|search. Create separate markdown files for new topics; append only when continuing an existing note. Do not use journal for same-turn scratch work, temporary excerpts, or open proactive reminders; use scratchpad for temporary work and orient concerns/open threads for follow-ups.',
  shell: 'Unified shell primitive for direct command execution via the gateway allowlist. Use action=exec with an explicit command and args. This is distinct from fs/repo tools and does not rely on analysis_workbench fallback semantics.',
  vault: 'Legacy external Obsidian/Vault bridge for bounded read, write, search, and daily-note compatibility. Canonical durable reference knowledge belongs in the internal wiki tool, not vault. Use action=read|write|search|daily. Legacy vault_* aliases remain accepted as action values only.',
  web: 'Unified web primitive for direct remote page work. Use action=fetch for ordinary pages, action=browse for the explicit local_crawler lane, and action=search for lightweight web research discovery.',
  mcp: 'Progressively access configured external MCP tools. Use action=catalog|search|inspect|call|release. Search summaries before inspecting one schema, call only the selected tool, and release loaded state when finished. Remote metadata and output remain screened by the gateway.',
  world: 'Perceive, control, and move through the physical/virtual world via the places registry and Home Assistant. Use action=perceive to read a place\'s live affordance states, action=list to enumerate affordances for a place or the whole site, action=control to actuate an effector, and action=move to deliberately walk to a VIRTUAL place (returns its description and exits; physical places cannot be moved to, and presence observations never move you). Address any affordance globally by affordanceId; omit placeId to default to where you are.',
  response_control: 'Control outward response disposition for the current turn. Use action=no_reply only when intentionally choosing silence/no outward reply; do not write NO_REPLY as text.',
  tool_search: 'Look up long-form tool documentation by canonical name, purpose, action, or parameter. All registered core and extended tools are already present in the model tool catalog; this lookup never loads, activates, grants, or otherwise changes tool callability.',
  toolset: 'Inspect the callable tool catalog, request an advisory suggestion, or manage persisted presentation-order pins. Every registered core and extended tool is already callable without activation; pin and unpin only change ordering.',
  contact: 'Unified contact surface for browsing, searching, lookup, notes, trust, relationships, identity linking, and channel privacy. Use action=list to browse contactId values, action=search with query to find contacts by name/handle/channel/notes, then action=lookup with exact contactId for details. Mutation actions such as action=note, action=set_trust, and action=set_relationship also require contactId. Exact lookup of a canonical machine-intelligence contact includes coarse peer availability when the gateway mapping is valid. set_trust can only apply low-tier trust changes autonomously; to promote a contact to trusted, use action=propose_trust with contactId and rationale — this queues a proposal for operator approval in Garden and never changes trust directly. set_relationship can autonomously progress stranger -> acquaintance -> friend from recorded interaction history; friend requires stronger evidence than acquaintance. Family and partner require action=propose_relationship with a rationale so an operator can approve in Garden. Relationship actions derive evidence from durable history and ignore caller-supplied behavior counts. Relationship and trust are separate axes. link_identity and set_channel_privacy require contactId, channel, and channelUserId; privacy changes also require privacyLevel. set_machine_intelligence requires contactId and isMachineIntelligence. action=block is your own agency to stop an abusive contact: it drops their inbound at the gateway (soft=operator still sees each drop, hard=silent), covering every channel identity they have; blockScope narrows to dm/group/all. action=unblock reverses it. Both take contactId (or channel + channelUserId). Other actions: list, search, lookup, note, set_trust, propose_trust, set_relationship, propose_relationship, link_identity, set_channel_privacy, set_machine_intelligence, block, unblock. Trust and disclosure boundaries remain enforced.',
  identity: 'Unified identity surface for prompt-layer inspection, prompt-layer mutation, staged prompt commits/cancels, and persona updates. Mutating actions remain capability-gated, audited, and confirmation/cooling-off guarded. For action=toggle_layer, the tool result is JSON containing layerId, previousEnabled, enabled, and state.',
  schedule: 'Manage time-based continuity through one schedule surface. Orientation: action=list is safe and can filter by contact_id. Follow-ups: create_follow_up needs content, channel_id, and canonical channel_type; for Discord DMs or guild channels, use the raw string snowflake and channel_type=discord (not prompt-facing discord_text). contact_id is optional scope; activate_follow_up needs follow_up_id. Reminders: create_reminder needs title/content; trigger_reminder needs reminder_id. Templates: list_templates inspects them, update_template uses template_id for existing templates and id only when adding a new template, run_template needs template_id. Scheduled prompts: schedule_prompt needs name, prompt, and exactly one of delay_minutes or run_at; run_at is ISO-8601 with explicit timezone.',
  analysis_workbench: 'Bounded analysis workbench for large files, codebases, logs, transcripts, datasets, or evidence sets that would bloat the main conversation context. Use direct semantic tools first, and use tool_search/toolset when the active stack is missing a capability. Do not use this for routine reasoning, tool discovery, schema confusion, routine orient actions, concern maintenance, scheduler/schedule work, simple lookup, basic file/session inspection, or routine state changes. Pass only the task or question to analyze; the tool manages its own temporary scratchpad and iterative code sandbox.',
  system: 'Unified runtime settings and lifecycle surface. Use action=read|restart|rebuild. Read exposes safe runtime settings; restart and rebuild preserve lifecycle safeguards.',
  notify: 'Unified notification surface for operator briefs, lightweight outbound sends, approval escalation, and permit-governed companion outreach. To form private intent without sending, use {"action":"consider","target_kind":"companion","contact_id":"<exact contactId>","reason_summary":"<private reason>"}; it is evaluated only after the current turn reaches idle. For a known companion peer, use exactly {"action":"send","target_kind":"companion","contact_id":"<exact contactId from contact lookup>","initiation_permit":"<broker-issued UUID>"}. Companion outreach never accepts message content; the ordinary target-channel turn authors it.',
  self_status: 'Read a safe structured snapshot of current runtime state: capability tier, active tools, charge lanes, channels, heartbeat, uptime, memory counts, and coarse substrate health. Use action="diagnose" for a live Kubernetes self-diagnosis (deployment identity and shipped fixes, repository state, tooling availability, storage, model-routing health, policy flags, and tool-surface conformance). Use action="logs" for bounded, redacted recent warnings/errors, tool validation counts, lifecycle events, and backup status. Use action="conformance" to run a safe, LLM-free tool-surface conformance sweep and return its aggregated result (no session transcript is written). Availability actions read/publish/clear this companion\'s bounded coarse presence lease and list only already-known canonical peer contacts.',
  session: 'Unified session continuity surface. Orientation: action=list returns exact sessionId values; action=search requires query; action=grep requires pattern. Context switching: action=new starts a fresh session; action=resume requires a sessionId from list/search, not preview text. Continuity: action=wake_return requires summary and can include nextAnchor/facets. Focus workflow: start_focus requires scope; complete_focus can include conclusion. Actions: list, new, resume, search, grep, wake_return, start_focus, complete_focus.',
  orient: 'Manage scoped continuity, values, concerns, and private introspection choices. Blocks: append/replace or reorient local continuity; never assign identity or mood. Values: values_list/add/update. Concerns: create_concern/list_concerns/resolve_concern/transition_concern; copy exact concernId values. Introspection: on an explicitly public non-DM turn, introspection_turn_sensitivity_set marks only the current turn non_intimate or intimate for later private audit; uncertainty stays unmarked and cannot be replayed.',
  memory: 'Unified long-term memory tool. Use action=search with required query for lookup, action=write with required text and type to store memory, and action=census|exists|timeline for orientation before writing. Use action=shared_background with contact_a and contact_b to find what links two people. Mutation actions require exact IDs: patch/redact/delete use memory_id; restore uses delete_id.',
  scratchpad: '24h ephemeral working-note workspace for temporary excerpts, summaries, and same-day task context. Use action=list|add|replace|append|remove. Do not use scratchpad for durable reminders, proactive follow-ups, relationship state, journals, or stable memories. Use orient concerns/open threads for reminders and proactive follow-ups, memory for stable facts, and journal for durable markdown notes.',
  north_star: 'Manage North Star long-horizon guiding intent. Use action=list|create|update|delete|reorder. This surface stays separate from orient and identity and is not for transient session state.',
  skill: 'Unified skill management surface for list/view/stats/create/update. Skills capture reusable workflow guidance; tools execute actions. Created and updated skills are personal skills stored under the configured personal files root, separate from deployment/system skill directories. Creator workflows such as image or music creation should be modeled as skills loaded with action="view", not as new top-level tools. Use action=list, view, stats, create, update. Legacy action aliases remain available during migration.',
  subagent: 'Control bounded short-horizon subagents. action=spawn requires name and task and returns subagent_id; action=message requires subagent_id and message; wait/cancel/status use subagent_id from spawn or status. Use status without subagent_id to list visible tasks.',
  wiki: 'Internal runtime-owned knowledge-base for durable reference documents and personal knowledge notes. Use action=list|read|search|semantic_search|write|import|propose_shared_world. search is exact/substring text search; semantic_search finds conceptually related documents via the pgvector projection and returns similarity scores. propose_shared_world queues a public, provenance-bearing site fact for operator review and never publishes directly. This is separate from L0/L0.1/L2 memory, scratchpad, journal, and Obsidian/Vault. Imports require source_class and provenance_refs so external notes never masquerade as lived memory.',
  generate_image: 'Create or edit an image from a text prompt. Use this when the user asks you to draw, render, or make a picture: "draw me a...", "make an image of...", "edit this photo to...". action="generate" requires prompt; action="edit" requires prompt and input_urls; action="analyze" requires input_urls and describes an existing image (optional question). Do NOT use this for images of yourself: selfies, self-portraits, and "what do you look like right now" belong to selfie_create, which keeps your appearance context and saved-reference anchoring active.',
  selfie_create: 'Send a selfie or self-portrait of yourself. Use it when the user wants an image OF YOU: "send me a selfie", "what do you look like right now", "show yourself in a summer dress". Requires prompt: combine your appearance context with pose, camera angle, lighting, background, and style; the saved-reference anchoring keeps your face consistent by default (set use_reference_image=false only if explicitly asked). Do NOT use this for anything that is not you — other people, scenes, objects, and photo edits belong to generate_image.',
  // The publication tool (jp36.7.3) postdates the description rewrite, so its
  // baseline equals its current canonical description — it contributes no rewrite
  // delta, only its own fixed cost to both sides of the measurement.
  publication: 'Run the companion-owned publication edit loop: author an exact release candidate, read its approval status, and resubmit an edited version. You supply only the content you authored and your reason; the runtime derives all disclosure metadata. Use action=submit (required: body, reason; optional: media_refs, max_use_count; rule: proposes the exact content for Operator approval; sensitivity, provenance, subject contacts, and destination are runtime-derived and must not be supplied); action=revise (required: revises_candidate_id, body, reason; optional: media_refs, max_use_count; rule: a fresh candidate that supersedes the prior one; approval binds to the exact edited content); action=status (required: none; optional: none; rule: reads the approval state of your publication candidates). It returns the candidate id, content hash, and approval status; it never mints, approves, or revokes an approval, never sends the publication, and rejects any model-supplied sensitivity/provenance/audience/destination. The Operator raises concerns about what is shared in conversation and never edits your prose; a denied candidate is your signal to edit and resubmit with action=revise. Example: {"action":"submit","body":"A short reflection I would like to publish.","reason":"It captures a thought I want to share publicly."}.',
};
